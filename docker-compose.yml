services:

  api:
    build: .
    ports:
      - "3005:3005"
    depends_on:
      - mysql

  mysql:
    image: mysql:8
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: ws_ecolim
    ports:
      - "3306:3306"
